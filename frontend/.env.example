# Backend API base URL (no trailing slash). WebSocket URL is derived from
# this automatically (http -> ws, https -> wss).
# Defaults to http://localhost:8000 for local development if unset.
NEXT_PUBLIC_API_URL=http://localhost:8000
